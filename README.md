# Custom tracers prototype

This repository contains a prototype of a module to run `debug_traceTransaction`'s custom tracers using QuickJS.

To run it, just install the project and `npx ts-node index.ts`.
